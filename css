/* Reset básico */
* { margin: 0; padding: 0; box-sizing: border-box; font-family: Arial, sans-serif; }

/* Navbar */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background-color: #4f46e5; 
    color: white;
}
.navbar .menu { list-style: none; display: flex; gap: 1rem; }
.navbar a { color: white; text-decoration: none; font-weight: bold; }

/* Hero */
.hero {
    text-align: center;
    padding: 4rem 2rem;
    background-color: #c7d2fe;
}
.hero h1 { font-size: 2.5rem; margin-bottom: 1rem; }
.hero p { margin-bottom: 2rem; font-size: 1.2rem; }
.btn-primary { padding: 0.8rem 1.5rem; background-color: #4f46e5; color: white; text-decoration: none; border-radius: 5px; }

/* Features */
.features-container { display: flex; justify-content: space-around; flex-wrap: wrap; padding: 2rem; gap: 2rem; }
.feature { background: #e0e7ff; padding: 1.5rem; border-radius: 10px; width: 250px; text-align: center; }

/* Pricing */
.pricing-cards { display: flex; justify-content: center; gap: 2rem; padding: 2rem; flex-wrap: wrap; }
.card { background: #f3f4f6; padding: 2rem; border-radius: 10px; text-align: center; width: 250px; }
.btn-secondary { display: inline-block; margin-top: 1rem; padding: 0.5rem 1rem; background-color: #6366f1; color: white; text-decoration: none; border-radius: 5px; }

/* Contact */
form { display: flex; flex-direction: column; gap: 1rem; width: 300px; margin: 0 auto; }
input { padding: 0.7rem; border-radius: 5px; border: 1px solid #ccc; }
button { padding: 0.8rem; background-color: #4f46e5; color: white; border: none; border-radius: 5px; cursor: pointer; }

/* Footer */
footer { text-align: center; padding: 1rem; background-color: #4f46e5; color: white; margin-top: 2rem; }

/* Responsive */
@media(max-width: 768px){
    .features-container, .pricing-cards { flex-direction: column; align-items: center; }
}